
- **Strict Schema Synchronization**: Database schema (PostgreSQL) and Python Model definitions (SQLAlchemy) must always be strictly synchronized. Any changes to one must immediately be reflected in the other via explicit migrations or sync scripts.

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-swarm-mcp"
version = "1.0.0"
description = "Model Context Protocol Vector Server"
dependencies = [
    "pytest",
    "mypy"
]
